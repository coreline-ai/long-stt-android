# implement_20260814_080041.md

작성 일시: `2026-08-14 08:00:41 KST`

이 문서는 현재 환경에서 안전하게 자동화할 수 있는 P1/P2 잔여 검증을 끝까지 수행하고, 사용자 조작·실데이터·외부 전송이 필요한 항목과 명확히 분리하기 위한 작업 문서다.

## 개발 목적

- 최신 `main` 기준으로 전체 자동 품질 게이트와 격리 실기기 회귀를 다시 검증한다.
- 잠금 해제된 Samsung 실기기에서 비민감 합성 근거 확인 → 대화 복귀 GUI를 실제 터치로 확인한다.
- 앱 기본 패키지와 사용자 데이터를 보존하면서 완료 알림·OAuth Keystore instrumentation을 격리 패키지에서 실행한다.
- 현재 환경에서 검증할 수 없는 출시 위험은 통과로 위장하지 않고 대기 조건을 구체적으로 남긴다.

## 개발 범위

- 앱/OAuth unit·Robolectric, Debug/Release lint, Debug/Release/deviceTest 빌드
- 16KB alignment, Release surface, asset provenance, `git diff --check`
- unsigned Release APK와 unsigned AAB 생성 가능 여부 확인
- Samsung SM-S931N에서 실행 중 장시간 작업이 없는 경우에만 데이터 보존 Debug 설치
- `com.stt.benchmark.deviceTest` 완료 알림 instrumentation과 OAuth test package의 합성 credential instrumentation
- Debug 전용 합성 채팅 Activity의 근거 chip, 강조 viewer, 상·하단/Android back 복귀와 200% 글자 GUI 확인
- 기존 저장공간·공유·API 수준 실패 계약의 자동 테스트 커버리지 확인 및 재현 결함의 최소 수정
- README, 최신 HANDOFF, 본 계획에 실제 결과와 잔여 위험 반영

## 제외 범위

- 실제 OAuth 로그인·만료 유도·로그아웃·재로그인
- 실제 외부 LLM 요청과 실제 사용자 전사 전송
- 요약/TXT의 외부 앱 최종 발송
- 사용자 장시간 원본의 새 import/STT와 의도적 저장공간 고갈
- 실제 화면 잠금, Bluetooth/USB 연결, 전화·알람 interruption 조작
- Release signing, 앱 스토어/제3자 배포
- 앱 데이터 초기화·uninstall·`pm clear`, 사용자 원문·경로·계정 정보 출력
- `.commandcode/` 수정·스테이징, 커밋·푸시

## 참조 문서

- [README](../README.md)
- [최신 HANDOFF](../docs/HANDOFF_20260810.md)
- [P1 실기기 안정성 계획](implement_20260813_085750.md)
- [P2 전사 채팅 계획](implement_20260813_100053.md)
- [근거 확인·복귀 계획](implement_20260813_160122.md)
- [완료 CTA 복원 계획](implement_20260812_174157.md)
- [전체 전사·공유 기준선](implement_20260812_141045.md)

## 공통 진행 규칙

- 각 Phase는 앞선 Phase의 자체 테스트 완료 후에만 시작한다.
- 구현 중 발생한 이슈는 해당 Phase에서 증상 → 재현 조건 → 코드 원인 → 수정 범위 → 회귀 위험 순서로 기록한다.
- 체크박스 상태는 실제 실행 결과와 맞게 갱신한다.
- 문서에 없는 기능 확장이나 navigation 재작성은 하지 않는다.
- 실행 중 Recorder/STT 작업이 감지되면 기본 앱 설치·실행·강제 종료를 하지 않는다.
- 실기기 자동 검증은 비민감 합성 상태와 별도 `deviceTest` 패키지만 사용한다.
- 전사 원문, segment, session ID, 오디오·모델 경로, token, 계정 식별자는 읽거나 출력하지 않는다.
- 기본 앱 설치가 필요하면 `adb install -r -t`만 사용하고 데이터 보존 여부는 aggregate 상태로만 확인한다.
- 실제 계정·외부 앱·외부 장비·사용자 원본이 필요한 항목은 자동 결과와 분리해 대기로 기록한다.
- 기존 프로젝트 API와 테스트 seam을 우선 사용하며 새 의존성을 추가하지 않는다.

## 엔지니어링 검토

1. **Verdict: ready**
2. **Source of truth:** 기능 계약은 현재 `main` 코드와 P1/P2 계획이며, 실기기 증거는 합성 debug/deviceTest 결과만 사용한다.
3. **최상위 위험:** 기본 앱 설치·Activity 실행이 사용자의 장시간 foreground 작업을 중단할 수 있으므로 service 상태를 먼저 읽기 전용으로 확인한다.
4. **격리 계약:** 알림 검증은 `com.stt.benchmark.deviceTest`, OAuth 검증은 테스트 전용 package에서 실행하고 기본 앱 private data를 열지 않는다.
5. **GUI 계약:** citation 검증은 고정 합성 문구만 가진 Debug Activity에서 수행하며 viewer를 닫아도 chat route·draft·mode가 유지되어야 한다.
6. **실패 계약:** 잠금·연결 해제·instrumentation 설치 실패는 기본 앱을 변경해 우회하지 않고 대기 또는 미검증으로 남긴다.
7. **배포 계약:** unsigned APK/AAB 생성과 정적 gate 통과는 signing·권리 승인·배포 채널 검증을 의미하지 않는다.
8. **테스트 계약:** 좁은/큰 글자, 예외 경계, 전체 gate, 격리 실기기 smoke를 순서대로 실행하고 실패한 계약만 최소 수정한다.

## Phase 상태 요약

- [x] Phase 1. 기준선·안전 상태 확인
- [x] Phase 2. 전체 자동 품질 게이트
- [x] Phase 3. 격리 Samsung 실기기 검증
- [x] Phase 4. 문서·잔여 위험 반영

## QA 관점

- [x] 기본 앱 package·private data가 instrumentation으로 변경되지 않는다.
- [x] 실행 중 장시간 Recorder/STT 작업이 있으면 앱 재설치·강제 종료를 건너뛴다.
- [x] citation viewer의 강조와 상단·하단·Android back 복귀가 동일한 대화를 유지한다.
- [x] 200% 글자·360dp급 화면에서 citation과 복귀 action에 도달한다.
- [x] 공유 handler 없음, 쓰기 실패, provider 실패와 저장 취소가 crash 없이 종료되는 자동 계약을 확인한다.
- [x] API 26 최소 지원과 최신 target API 분기 계약을 가능한 자동 범위에서 확인한다.
- [x] unsigned 산출물과 실제 릴리스 절차를 명확히 구분한다.
- [x] 실제 외부 전송·실계정·하드웨어 항목이 자동 통과로 표시되지 않는다.

## Phase 1. 기준선·안전 상태 확인

### 목표

- 최신 Git·문서·장치 상태와 실행 가능한 자동 검증 범위를 확정한다.

### 구현 태스크

- [x] README, 최신 HANDOFF, P1/P2/근거 복귀 계획을 확인한다.
- [x] `main`/`origin/main` 일치와 `.commandcode/` 외 사용자 변경이 없음을 확인한다.
- [x] 자동화 가능 항목과 사용자 조작 대기 항목을 분리한다.
- [x] Samsung 연결·잠금·기본 앱 foreground service 상태를 민감정보 없이 확인한다.

### 자체 테스트

- [x] 계획이 기본 앱 데이터 삭제·실데이터 출력·외부 발송을 포함하지 않는다.
- [x] 격리 package와 기본 package의 경계가 명시되어 있다.

### 이슈 및 수정

- [x] 발견 이슈 없음

### 완료 조건

- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능

## Phase 2. 전체 자동 품질 게이트

### 목표

- 최신 소스의 기능·보안·산출물 회귀를 자동 검증한다.

### 구현 태스크

- [x] 앱 unit/Robolectric 테스트와 OAuth 43건을 실행한다.
- [x] Debug/Release lint와 Debug/Release/deviceTest/androidTest assemble을 실행한다.
- [x] 16KB alignment, Release surface, asset provenance를 실행한다.
- [x] unsigned Release AAB를 생성하고 산출물만 확인한다.
- [x] 저장·공유 실패와 API 분기 관련 기존 테스트 커버리지를 확인한다.
- [x] `git diff --check`를 실행한다.

### 자체 테스트

- [x] 테스트 실패가 0건이다.
- [x] Debug APK, unsigned Release APK/AAB, deviceTest APK가 생성된다.
- [x] Release 산출물에 debug audit surface가 없다.

### 이슈 및 수정

- [x] 발견 이슈: 완료 target·알림·TXT·요약/TXT 공유의 Robolectric 계약이 API 34에만 고정돼 minSdk 26 회귀를 직접 실행하지 않았다.
- [x] 수정 기록: 프로덕션 코드 변경 없이 7개 핵심 test class를 API 26/34 양쪽에서 실행하도록 확장했다.
- [x] 발견 이슈: API 26에서 테스트가 API 31에 추가된 `PendingIntent.isImmutable` platform method를 호출해 `NoSuchMethodError`가 발생했다.
- [x] 수정 기록: API와 무관하게 생성 flags를 노출하는 Robolectric `ShadowPendingIntent.isImmutable` assertion으로 교체했고 확장 44건이 통과했다.
- [x] 회귀 위험: 새 SDK 조합이 테스트 수와 실행 시간을 늘리지만 프로덕션 binary와 동작은 바꾸지 않는다.
- [x] 발견 이슈: 확장 후 첫 전체 suite에서 그룹 저장소 `latestActive` assertion이 1회 실패했으나 격리 4건과 전체 247건 캐시 없는 재실행에서는 재현되지 않았다.
- [x] 수정 기록: 전역 Robolectric `filesDir`의 같은 하위 폴더를 두 test class가 정리하던 결합을 제거하고 그룹 저장소 테스트에 테스트별 임시 files root를 주입했다.
- [x] 회귀 위험: 변경은 테스트 context에만 한정되고 앱의 실제 private 저장 경로와 store 구현은 유지된다.

### 완료 조건

- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능

## Phase 3. 격리 Samsung 실기기 검증

### 목표

- 사용자 데이터와 외부 네트워크 없이 남은 합성 실기기 계약을 검증한다.

### 구현 태스크

- [x] 기본 앱 foreground 작업이 없을 때만 Debug APK를 `adb install -r -t`로 설치한다.
- [x] `deviceTest` 완료 알림 instrumentation을 실행하고 정확히 1건·immutable PendingIntent 계약을 확인한다.
- [x] OAuth synthetic credential 2건을 테스트 package에서 실행한다.
- [x] opt-in 없는 전체 deviceTest를 실행해 데이터 안전 테스트와 사용자 데이터/실마이크 gate의 skip 경계를 확인한다.
- [x] 합성 COMPLETED 채팅에서 citation chip → 강조 viewer → 상단 복귀를 확인한다.
- [x] 같은 흐름에서 하단 복귀와 Android back 복귀를 확인한다.
- [x] 200% 글자 합성 화면에서 citation/viewer/복귀 action 도달성을 확인한다.
- [x] 실행 뒤 기본 앱 Recorder/STT service와 held wake lock을 generic 상태로만 확인한다.

### 자체 테스트

- [x] instrumentation이 모두 통과하고 테스트 package 외 사용자 데이터를 변경하지 않는다.
- [x] 전체 deviceTest 13건 중 비민감 자동 8건 통과, 실제 마이크/AICore 5건은 예상대로 건너뛴다.
- [x] citation viewer 복귀 뒤 합성 질문·답변과 mode가 유지된다.
- [x] 실제 OAuth·LLM·외부 앱 요청이 0건이다.

### 이슈 및 수정

- [x] 발견 이슈: OAuth test APK 설치·실행 결합 명령의 자동 승인 검토가 두 번 시간 초과됐다.
- [x] 수정 기록: ADB server를 정상 재시작하고 이미 설치된 test-only package를 확인한 뒤 instrumentation 실행만 분리해 `OK (2 tests)`를 확인했다.
- [x] 발견 이슈: 하단 복귀 첫 터치 좌표가 Android IME 입력 영역과 겹쳐 viewer가 유지됐다.
- [x] 수정 기록: 안정된 viewer의 실제 버튼 중심을 사용해 하단 복귀를 재검증했고 대화 질문·답변·mode 보존을 확인했다. 앱 코드 결함은 재현되지 않았다.

### 완료 조건

- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능

## Phase 4. 문서·잔여 위험 반영

### 목표

- 실행 결과와 사람/하드웨어가 필요한 게이트를 다음 작업자가 오인 없이 이어받게 한다.

### 구현 태스크

- [x] README 검증 표와 남은 검증을 실제 결과로 갱신한다.
- [x] 최신 HANDOFF에 명령·결과·산출물 hash와 안전 경계를 갱신한다.
- [x] 본 계획 체크박스·이슈·잔여 위험을 실제 상태로 갱신한다.

### 자체 테스트

- [x] 테스트 수와 산출물 상태가 실제 출력과 일치한다.
- [x] 사용자 조작 대기 항목이 통과로 표시되지 않는다.
- [x] `.commandcode/`가 변경·스테이징되지 않는다.

### 이슈 및 수정

- [x] 발견 이슈 없음

### 완료 조건

- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 최종 결과·잔여 위험 반영 완료

## 최종 결과 요약

- 앱 `247 passed`, OAuth `43 passed`, 전체 `290 passed`이며 실패·오류·건너뜀은 0건이다.
- 완료 target·완료 알림·TXT 저장·TXT/요약 공유 핵심 Robolectric 계약 44건을 API 26과 34에서 실행했다.
- Debug/Release lint, Debug/Release/deviceTest/androidTest assemble, OAuth androidTest APK, 16KB alignment, Release surface, asset provenance, unsigned Release AAB, `git diff --check`가 통과했다.
- Samsung SM-S931N에서 완료 알림 `OK (1 test)`, OAuth Keystore `OK (2 tests)`, 합성 citation 강조·상단/하단/Android back 복귀와 200% 글자 도달성을 확인했다.
- 격리 deviceTest 전체는 비민감 8건이 통과했고 실제 마이크 4건·AICore 사용자 원본 probe 1건은 opt-in 없이 안전하게 건너뛰었다.
- 최종 Debug APK를 `adb install -r -t`로 설치했고 설치본 hash가 로컬 `2fd81a6a11c4fd758ad70671c92a662f7c7a6a60cebf10bd50be7e9e01c69423`와 일치하며 MainActivity cold start가 성공했다.
- 실제 OAuth·외부 LLM·외부 앱·사용자 원본 요청은 0건이며 Recorder/STT service와 관련 wake lock 잔류도 0건이다.

## 계획 실행 시점의 잔여 위험 / 후속 과제

- 실제 OAuth 로그인·재실행 복원·401 refresh·로그아웃 lifecycle은 사용자 조작 대기다.
- 실제 승인된 비민감 전사의 외부 LLM 응답 품질·지연·비용과 실제 section 착지는 사용자 승인 대기다.
- 실제 외부 앱으로 요약/TXT 최종 전달은 사용자 승인 대기다.
- 실제 secure lock, Bluetooth/USB route 변경, 전화·알람 interruption은 사용자/하드웨어 대기다.
- 승인된 장시간 원본의 새 import/STT, 의도적 저장공간 부족, process recreation은 원본·사용자 조작 대기다.
- unsigned APK/AAB는 signing·배포 채널 검증을 대체하지 않는다.

## 사후 완료 기록 — 2026-08-14

이 계획의 자동·합성 검증 이후 프로젝트 담당자가 위 사용자·실장비 검증 항목을 모두 완료한 것으로 확인했다. 민감한 계정·전사 원문·외부 대상·원시 로그는 저장소에 기록하지 않는다. 실제 릴리스는 Coreline이 결정하며, 선택한 배포 채널에 맞춰 signed 산출물과 제3자 고지·라이선스 및 적용 서비스 약관을 확인한다. 최신 상태는 [`VERIFICATION_COMPLETION_20260814.md`](../docs/VERIFICATION_COMPLETION_20260814.md)를 기준으로 한다.
