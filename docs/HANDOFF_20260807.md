# LongSTT Android 개발 핸드오프

작성 기준: `2026-08-07 16:47:17 KST`

이 문서는 현재 저장소, 삼성폰, 6시간 전사 데이터, 미완료 계획과 다음 구현 순서를 한곳에 고정한다. 체크된 개발 계획만 믿지 말고 아래의 `실제 확인`, `계획`, `미검증` 구분을 유지한다.

## 1. 현재 결론

- 프로젝트의 1차 목적은 삼성폰에서 6시간급 WAV/M4A/MP3 전사를 누락 없이 완주하고 결과를 안전하게 보존하는 것이다.
- 장시간 STT, 10분 청크 처리, Foreground Service, checkpoint, 전사 결과 보관함, 오디오·Whisper 모델 관리 코드는 현재 앱에 존재한다.
- 삼성 SM-S931N에는 `com.stt.benchmark` 1.0.0이 설치되어 있고 핸드오프 시점에 `MainActivity`가 화면에 열려 있다.
- 완료된 6시간 전사 checkpoint가 삼성폰에 보존되어 있으며 파일 SHA-256을 다시 확인했다.
- Codex OAuth와 외부 LLM 요약 코드는 현재 프로젝트에 아직 없다.
- 다음 개발은 검증된 `alpine-llm-gateway` Android OAuth 소스를 별도 `:codex-oauth-android` 모듈로 직접 포팅하는 작업부터 시작한다.
- 별도 Gateway와 `codex app-server`를 먼저 만드는 계획은 폐기하지 않고 이력으로만 보존하며, 현재 실행 계획으로 사용하지 않는다.
- 온디바이스 AICore/ML Kit 요약은 현재 삼성폰에서 사용할 수 없음이 확인되어 정식 경로로 진행하지 않는다. 관련 코드는 debug/androidTest 사전 probe일 뿐이다.

## 2. 프로젝트 식별 정보

| 항목 | 현재 값 |
|---|---|
| 프로젝트 이름 | LongSTT Android |
| 로컬 경로 | `/Users/iriver/Documents/glm/AndroidSttBenchmark` |
| Git 원격 | `https://github.com/coreline-ai/long-stt-android.git` |
| 브랜치 | `main` |
| HEAD | `12c94833ed689c33e6dfe7ef042fe980145a02bd` |
| 원격 상태 | `HEAD == origin/main` |
| Git 작성자 | `corelineai` |
| Git 이메일 | `corelineai@users.noreply.github.com` |
| Android applicationId | `com.stt.benchmark` |
| 앱 표시 이름 | `STT 벤치마크` |
| 앱 버전 | `versionCode=1`, `versionName=1.0.0` |
| Android SDK | minSdk 26, compileSdk 34, targetSdk 34 |
| 주요 언어/UI | Kotlin 1.9.22, Java 17, Jetpack Compose Material3 |
| STT 엔진 | whisper.cpp JNI, arm64-v8a |
| 주 검증 기기 | Samsung SM-S931N |
| 주 기기 ADB serial | `R3CY40PXCAP` |

로컬 디렉터리 이름은 과거 이름인 `AndroidSttBenchmark`이고 원격 저장소 이름은 `long-stt-android`다. 경로를 임의로 변경하지 않는다.

## 3. 삼성폰 현재 상태

`2026-08-07 16:46~16:47 KST` 읽기 전용 ADB 확인 결과다.

- 연결 기기 1: `R3CY40PXCAP`, `SM_S931N` — 주 검증 대상
- 연결 기기 2: `H84225J181G02072`, `IM_H842` — 이번 작업 대상 아님
- 삼성폰 설치 패키지: `com.stt.benchmark`
- 삼성폰 테스트 패키지: `com.stt.benchmark.test`
- 설치 버전: `1.0.0 (1)`
- 실행 프로세스가 존재하고 `com.stt.benchmark/.MainActivity`가 `topResumedActivity`였다.
- 활성 `TranscriptionService`는 감지되지 않았다. 즉 앱 화면은 열려 있지만 핸드오프 시점에 Foreground 장시간 전사는 실행 중이 아니었다.

두 기기가 동시에 연결되어 있으므로 모든 ADB 명령에 반드시 아래처럼 삼성폰 serial을 지정한다.

```bash
adb -s R3CY40PXCAP devices
adb -s R3CY40PXCAP shell pidof com.stt.benchmark
```

새 APK 설치 전에는 다시 `TranscriptionService` 비활성 상태와 checkpoint hash를 확인한다. 이 문서 작성 중에는 새 APK를 설치하지 않았다.

## 4. 보존해야 하는 6시간 전사 기준 데이터

문서화된 기준 세션은 다음과 같다.

| 항목 | 값 |
|---|---|
| source session | `stt_1786079390245_83ec4015` |
| 오디오 길이 | `6:13:23.578` |
| 청크 | `38/38` |
| 세그먼트 | `7,679` |
| 통합 텍스트 | `166,870`자 |
| checkpoint 파일 크기 | `1,153,322` bytes |
| checkpoint SHA-256 | `0a8b0a524bf5d770a75133aefbf8df84d7c08791c2e15a72572fa7f8ab9a741e` |
| 기기 내부 경로 | `files/stt_sessions/stt_1786079390245_83ec4015.json` |

핸드오프 작성 시 삼성폰 내부 파일의 SHA-256을 다시 계산했고 위 값과 일치했다. source session의 통계는 [6시간 외부 요약 기록](EXTERNAL_LLM_SUMMARY_6H.md)에 기록된 값이다.

현재 `files/stt_sessions/`에는 위 파일 외에 약 39KB 크기의 세션 파일 2개가 더 있다. 요약 개발 중에는 `TranscriptionSessionStore`를 읽기 전용 source-of-truth로 취급하고 세 파일을 삭제하거나 다시 저장하지 않는다.

무결성 재확인 명령:

```bash
adb -s R3CY40PXCAP shell run-as com.stt.benchmark \
  toybox sha256sum files/stt_sessions/stt_1786079390245_83ec4015.json
```

## 5. 현재 앱에 실제 구현된 기능

### STT 실행

- `MainActivity`가 일반 실행과 ADB 자동 실행 extra를 `SttViewModel`로 전달한다.
- `WhisperCppEngine`이 모델 로드, PCM 전사, segment 결과, native release를 담당한다.
- `AudioDecoder`가 WAV와 Android `MediaExtractor/MediaCodec` 기반 압축 오디오 구간 디코딩을 담당한다.
- 메인 장시간 경로는 `TranscriptionService`에서 실행된다.
- 서비스는 `START_STICKY`, Foreground notification, partial WakeLock을 사용한다.
- 기본 청크는 10분이다.
- 청크 양쪽에 1초 overlap을 디코딩하고 primary 시간 범위의 segment만 결과에 포함한다.
- 청크 실패는 최대 2회 시도한다.
- 청크 사이 기본 냉각은 10초이고 5번째 청크마다 30초 냉각한다.
- 실행 전 앱 내부 여유 공간 256MiB 이상을 요구한다.
- 처리 완료 후 coverage를 검증하며 허용 오차는 50ms다.

주요 파일:

- [`MainActivity.kt`](../app/src/main/java/com/stt/benchmark/MainActivity.kt)
- [`SttViewModel.kt`](../app/src/main/java/com/stt/benchmark/ui/SttViewModel.kt)
- [`TranscriptionService.kt`](../app/src/main/java/com/stt/benchmark/service/TranscriptionService.kt)
- [`WhisperEngine.kt`](../app/src/main/java/com/stt/benchmark/whisper/WhisperEngine.kt)
- [`AudioDecoder.kt`](../app/src/main/java/com/stt/benchmark/whisper/AudioDecoder.kt)

### Checkpoint와 전사 결과

- `TranscriptionSessionStore`가 `files/stt_sessions/<sessionId>.json`에 저장한다.
- 상태는 `PREPARING`, `RUNNING`, `COOLING`, `COMPLETED`, `FAILED`, `CANCELLED`, `INTERRUPTED`다.
- 청크별 primary/decoded 범위, sample 수, retry, elapsed, text, segment를 저장한다.
- 저장에는 Android `AtomicFile`을 사용한다.
- 앱 재진입 시 최신 미완료 세션을 찾아 Foreground Service로 재개한다.
- 결과 보관함은 session JSON을 기준으로 조회하고, 활성/재개 대상이 아닌 결과는 삭제할 수 있다.
- CSV는 전사 본문의 source-of-truth가 아니라 별도의 성능 측정 기록이다.

주요 파일:

- [`TranscriptionSessionStore.kt`](../app/src/main/java/com/stt/benchmark/data/TranscriptionSessionStore.kt)
- [`BenchmarkRecorder.kt`](../app/src/main/java/com/stt/benchmark/data/BenchmarkRecorder.kt)

### 오디오와 모델 관리

- 가져온 오디오는 앱 내부 저장소로 복사하고 `media_library.json`에 메타데이터와 선택 상태를 원자 저장한다.
- 목록 재선택, 목록에서 숨기기, 앱 내부 원본 영구 삭제가 분리되어 있다.
- 인식하는 오디오 확장자는 MP3, WAV, M4A, AAC, MP4, OGG, FLAC다.
- Whisper 모델은 Hugging Face의 `ggerganov/whisper.cpp`에서 앱 내부 `files/models/`로 다운로드한다.
- tiny, base, base-q5_1, small, small-q5_1, medium 모델 목록이 있다.
- 설치 모델 선택/재로드/삭제 GUI가 있다.

주요 파일:

- [`MediaLibraryStore.kt`](../app/src/main/java/com/stt/benchmark/data/MediaLibraryStore.kt)
- [`ModelDownloader.kt`](../app/src/main/java/com/stt/benchmark/data/ModelDownloader.kt)
- [`SttBenchmarkScreen.kt`](../app/src/main/java/com/stt/benchmark/ui/SttBenchmarkScreen.kt)

## 6. 현재 빌드 기준점

핸드오프 작성 중 아래 명령을 실제 실행했다.

```bash
./gradlew :app:testDebugUnitTest :app:assembleDebug :app:assembleDebugAndroidTest
```

결과:

- `BUILD SUCCESSFUL`
- 74 tasks: 4 executed, 70 up-to-date
- `:app:assembleDebug` 성공
- `:app:assembleDebugAndroidTest` 성공
- `:app:testDebugUnitTest`는 성공으로 끝났지만 `NO-SOURCE`다. 현재 `app/src/test` 단위 테스트가 없다는 뜻이며 테스트 통과 증거로 해석하면 안 된다.
- AndroidTest APK는 조립만 했고 이번 핸드오프 작성 중 삼성폰에서 계측 테스트를 실행하지 않았다.
- Release APK와 16KB 정렬은 이번 핸드오프 작성 중 다시 검증하지 않았다.

현재 산출물:

| 산출물 | 크기 | SHA-256 |
|---|---:|---|
| `app/build/outputs/apk/debug/app-debug.apk` | 약 20MB | `4d41a2e2db264c94de32436c6c4b5f62f4ebc14493ebb14d9541d49bf29bb09f` |
| `app/build/outputs/apk/androidTest/debug/app-debug-androidTest.apk` | 약 2.4MB | `3f94d5c0d4263f3df7aead5c6286c64d690156a6112662a8e40e3d075ffac46e` |

두 APK는 빌드 시 `UP-TO-DATE`였고, 현재 삼성폰에 설치된 APK가 위 Debug APK와 byte-for-byte 동일하다고 확인하지는 않았다.

## 7. Git 작업 트리 상태

현재 브랜치는 `main`이고 원격 `origin/main`과 같은 초기 커밋을 가리킨다. 아래 변경은 아직 커밋되지 않았다.

```text
 M app/build.gradle.kts
?? app/src/androidTest/
?? app/src/debug/
?? dev-plan/implement_20260807_144941.md
?? dev-plan/implement_20260807_161201.md
?? dev-plan/implement_20260807_163104.md
?? dev-plan/implement_20260807_164038.md
?? docs/EXTERNAL_LLM_SUMMARY_6H.md
?? docs/HANDOFF_20260807.md
```

주의 사항:

- 기존 변경을 `git reset`, `git clean`, `git checkout --`로 지우지 않는다.
- `app/build.gradle.kts`, `app/src/debug`, `app/src/androidTest`는 AICore/ML Kit 사전 검증 작업이다.
- ML Kit summarization 의존성은 `androidTestImplementation`에만 있다. 정식 앱 기능으로 승격하지 않는다.
- 새 OAuth 작업은 위 변경과 파일 소유권이 겹치지 않도록 별도 library module부터 시작한다.
- 커밋 전에는 AICore probe 보존 여부와 OAuth Phase 1 변경을 논리적으로 분리하는 것이 좋다.

## 8. 요약 방식 결정 기록

### 온디바이스 요약

[온디바이스 계획](../dev-plan/implement_20260807_144941.md)의 Galaxy S25 probe는 실제 AICore 응답까지 도달했으나 Summarization feature가 provision되지 않아 한국어 모델 준비와 추론을 진행할 수 없었다.

- 현재 판정: 운영 기능으로 사용 불가
- probe 위치: `app/src/androidTest/.../OnDeviceSummaryProbeTest.java`
- debug 보조 Activity: `app/src/debug/.../SummaryProbeActivity.kt`
- 6시간 전사 checkpoint SHA-256은 probe 전후 동일했다.

### 외부 LLM 선행 요약

현재 Codex 작업 세션에서 6시간 전사를 38개 청크에서 약 30분 단위 13개로 나눠 전체를 읽고 계층 요약한 선행 결과가 있다. 정확한 전체 LLM 소요 시간은 기록하지 않았고, API key를 사용하는 OpenAI API 직접 호출도 하지 않았다.

- 기록과 프롬프트: [EXTERNAL_LLM_SUMMARY_6H.md](EXTERNAL_LLM_SUMMARY_6H.md)
- 재현 구조: 13개 unit 요약 → merge → final
- 앱 구현 시 unit별 checkpoint, source range, source hash, prompt/schema/model version, elapsed와 usage를 저장해야 한다.

### Codex OAuth 최종 방향

사용자는 `alpine-llm-gateway`의 기존 Android 앱에서 OAuth 로그인과 실제 Codex 챗봇 응답이 성공했음을 확인했다. 참조 README에도 삼성 SM-S931N 실제 검증 기록이 있다.

최종 판정은 `ready with parity gate`다.

- 활성 계획: [implement_20260807_164038.md](../dev-plan/implement_20260807_164038.md)
- 대체된 계획: [implement_20260807_163104.md](../dev-plan/implement_20260807_163104.md)
- 참조 저장소: `https://github.com/coreline-ai/alpine-llm-gateway.git`
- 고정 참조 commit: `3389fcbf92207bb632dac002d7bb501d0901ff22`
- 최초 parity 모델: `gpt-5.3-codex-spark`
- 고정 provider ID: `codex_summary_v1`
- 인증 흐름: Custom Tabs → localhost:1455 loopback → Authorization Code + PKCE/state → form token 교환 → Android Keystore AES-GCM → Codex Responses SSE

참조 저장소는 핸드오프 직전 다음 작업이 성공했다.

```text
:android:testDebugUnitTest
:alpine-chat-provider-android:testDebugUnitTest
:demo-chatbot:testDebugUnitTest
:demo-chatbot:assembleDebug

BUILD SUCCESSFUL in 35s
119 actionable tasks: 62 executed, 57 up-to-date
```

이는 참조 프로젝트의 빌드 증거다. 현재 STT 앱에서 OAuth 로그인이나 Codex 응답을 실행했다는 뜻은 아니다.

## 9. 다음 구현 작업

반드시 활성 계획의 Phase 순서를 따른다.

### Phase 1 — OAuth 소스 독립 모듈 포팅

1. `settings.gradle.kts`에 `:codex-oauth-android`를 추가한다.
2. compileSdk 34, minSdk 26, JVM 17의 Android library module을 만든다.
3. 참조 commit `3389fcb`의 Android OAuth/Codex 소스 19개를 package `dev.alpine.llm` 그대로 가져온다.
4. 참조 manifest의 `OAuthRedirectActivity`와 consumer rules를 가져온다.
5. Kotlin 2.2 AAR을 직접 사용하지 않고 현재 Kotlin 1.9.22/coroutines 1.7.3 환경에서 source를 재컴파일한다.
6. 현재 SDK와 호환되는 AndroidX Browser 버전을 실제 빌드로 고정한다.
7. upstream unit/instrumentation test를 포팅한다.
8. `codex-oauth-android/UPSTREAM.md`에 원본 commit, 파일, 변경점과 보류한 hardening을 기록한다.
9. Alpine runtime, proot, 전체 chat UI, 다른 provider 코드는 가져오지 않는다.

포팅 대상은 활성 계획의 `포팅할 참조 소스` 절에 고정되어 있다. 핵심 클래스는 다음과 같다.

```text
CodexOAuthContract.kt
OAuthProviderConfig.kt
OAuthPkce.kt
OAuthFailure.kt
OAuthManager.kt
OAuthCallbackServer.kt
OAuthCallbackRegistry.kt
OAuthRedirectActivity.kt
OAuthTokenStore.kt
OAuthRefreshCoordinator.kt
OAuthTokenRequestAdapter.kt
OAuthTokenResponseAdapter.kt
JwtClaimMetadataTokenResponseAdapter.kt
OAuthDiscovery.kt
HostLlmBridge.kt
OAuthHttpLlmBridge.kt
GatewayOperations.kt
ProviderProtocolAdapters.kt
CodexResponsesOAuthAdapter.kt
```

Phase 1 검증 명령:

```bash
./gradlew \
  :codex-oauth-android:testDebugUnitTest \
  :codex-oauth-android:assembleDebug \
  :app:assembleDebug
```

### Phase 2 — 현재 앱에서 실제 OAuth parity

- `CodexSummaryAuthController`와 앱 전용 factory/wrapper만 `com.stt.benchmark.summary`에 추가한다.
- upstream endpoint, client ID, scope, callback, header와 request body를 변경하지 않는다.
- MainActivity의 visible Activity로 Custom Tabs 로그인을 시작한다.
- `gpt-5.3-codex-spark`로 비민감 고정 문장 하나만 전송한다.
- 로그인, 앱 재시작 후 token 복원, refresh, 로그아웃을 삼성폰에서 검증한다.
- 실제 시험 전후 6시간 checkpoint SHA-256을 비교한다.
- OAuth parity가 실패하면 요약 구현이나 hardening으로 넘어가지 말고 upstream과의 diff부터 제거한다.

### Phase 3 이후

- parity 성공 후에만 redirect 차단, 저장 완료 보장, request/response 상한, 로그 redaction을 한 항목씩 적용한다.
- 6시간 source를 `38 → 13` unit으로 계획하고 별도 `SummarySessionStore(AtomicFile)`에 결과만 저장한다.
- unit 13회 → merge 1회 → final 1회 순서로 처리한다.
- 성공 unit은 즉시 checkpoint하고 실패·취소 후 성공 unit을 재호출하지 않는다.
- STT와 외부 요약은 동시에 실행하지 않는다.
- 사용자가 외부 전송 범위와 모델을 확인하고 동의한 뒤에만 원문을 전송한다.
- 요약 GUI에는 로그인, 진행 단계, 안전 저장, 실패, 재개, 결과, 요약만 삭제, 모델 allowlist 선택을 제공한다.

## 10. 보안 및 구현 경계

- access token, refresh token, authorization code, PKCE verifier와 state를 logcat, UI, Intent, summary JSON에 넣지 않는다.
- client secret이나 API key 입력 기능을 만들지 않는다.
- endpoint, client ID, scope, callback을 사용자가 편집하는 일반 provider UI를 만들지 않는다.
- Codex 요청에 tools, command 실행, 파일 변경, 브라우저 검색 기능을 넣지 않는다.
- token은 고정 token endpoint에만, access token은 고정 Responses endpoint에만 전달한다.
- inference POST는 명시적 idempotency 계약이 없으므로 자동 재시도하지 않는다.
- 401은 refresh 1회까지만 허용하고 다시 실패하면 재로그인으로 전환한다.
- 429, 5xx와 timeout은 현재 unit을 checkpoint한 뒤 명시적 재개 대상으로 남긴다.
- 기존 source의 account ID metadata는 provider header 호환에만 사용하고 앱 인증·권한 판단에 사용하지 않는다.
- 전사 원문 전체를 OAuth 저장소, 일반 로그, crash report 또는 summary checkpoint에 복제하지 않는다.
- endpoint/header/model의 upstream 변경 시 요약 기능만 비활성화하고 STT 기능은 유지한다.

## 11. 미완료 및 알려진 위험

- `app/src/test`가 없어 청크 계획, store, decoder, ViewModel 상태의 JVM 단위 회귀가 없다.
- 현재 6시간 완료 세션 한 건은 존재하지만 WAV/M4A/MP3 세 형식 전체의 공식 6시간 QA gate는 계획상 미완료다.
- 진행 GUI에는 기본 상태와 진행률이 있으나 계획된 상세 청크 지도, 단계별 안전 저장, ETA와 장애 복구 UX는 공식 완료 처리되지 않았다.
- TXT/Markdown/PDF export와 외부 TXT/Markdown/PDF import는 계획만 있고 코드가 없다.
- Codex OAuth, SummarySessionStore, SummaryPlanner, SummaryCoordinator와 요약 GUI는 모두 미구현이다.
- 참조 OAuth 경로는 실제 성공 증거가 있지만 upstream endpoint/header/model이 변경될 수 있다.
- 참조 source의 HTTP redirect와 비동기 preference 저장은 parity 성공 후 별도 hardening이 필요하다.
- 현재 Codex adapter는 `max_output_tokens`, `response_format`, strict structured output을 전달하지 않는다. 초기 구현은 prompt JSON 계약, validator, 최대 1회 교정 요청을 사용한다.
- 공개 배포 정책과 참조 client registration 재사용 범위는 내부 동작 성공과 별개로 출시 전에 확인해야 한다.
- ML Kit beta probe는 Kotlin metadata 호환 문제 때문에 test source set에 격리되어 있다.

## 12. 계획 문서 우선순위

| 우선순위 | 문서 | 상태 |
|---:|---|---|
| 1 | [Codex OAuth 직접 포팅·6시간 요약](../dev-plan/implement_20260807_164038.md) | 활성, 다음 구현 대상 |
| 2 | [6시간 전사](../dev-plan/implement_20260807_111158.md) | 부분 구현, 세 형식 최종 QA 미완료 |
| 3 | [전사 결과·오디오·모델·진행 GUI](../dev-plan/implement_20260807_133326.md) | 주요 CRUD 구현, 상세 진행 UX·QA 미완료 |
| 4 | [문서 가져오기·내보내기](../dev-plan/implement_20260807_161201.md) | 계획만 존재 |
| 이력 | [온디바이스 AICore 요약](../dev-plan/implement_20260807_144941.md) | 현재 기기에서 기능 unavailable |
| 대체됨 | [Gateway/app-server 요약](../dev-plan/implement_20260807_163104.md) | 활성 계획이 아님 |

체크박스는 일부 구현 사실을 나타내지만 전체 Phase 완료와 실기기 QA 완료를 뜻하지 않을 수 있다. 코드, 현재 빌드와 실기기 증거를 함께 확인한다.

## 13. 다음 담당자 시작 체크리스트

- [ ] `pwd`가 `/Users/iriver/Documents/glm/AndroidSttBenchmark`인지 확인한다.
- [ ] `git status --short`로 기존 미커밋 파일을 확인하고 보존한다.
- [ ] 활성 계획 `implement_20260807_164038.md`를 처음부터 읽는다.
- [ ] ADB 명령에 `-s R3CY40PXCAP`을 사용한다.
- [ ] 활성 STT service가 없는지 확인한다.
- [ ] 6시간 checkpoint SHA-256을 기록한다.
- [ ] `:codex-oauth-android` 모듈만 먼저 생성한다.
- [ ] upstream 19개 source와 tests를 package 변경 없이 포팅한다.
- [ ] source provenance를 `UPSTREAM.md`에 기록한다.
- [ ] library tests와 현재 app Debug build를 통과한다.
- [ ] parity 전에는 OAuth 동작을 리팩터링하거나 hardening하지 않는다.
- [ ] 삼성폰 실계정 로그인과 단일 비민감 응답을 확인한다.
- [ ] 시험 후 checkpoint SHA-256이 같은지 확인한다.
- [ ] 실제 실행 명령, 기기 결과, 실패와 수정 내용을 활성 개발 계획에 즉시 반영한다.

## 14. 핸드오프 완료 조건

다음 담당자는 우선 Phase 1과 Phase 2만 완료한 뒤 사용자에게 결과를 보고한다.

- `:codex-oauth-android`가 현재 프로젝트 toolchain에서 빌드된다.
- upstream OAuth/Codex unit tests가 포팅되어 통과한다.
- 현재 STT 앱의 Debug build가 회귀하지 않는다.
- 삼성 SM-S931N에서 기존 앱과 같은 OAuth 로그인이 성공한다.
- 비민감 Codex SSE 응답 1회가 성공한다.
- token과 OAuth 민감값이 UI/logcat/checkpoint에 없다.
- 기존 6시간 checkpoint SHA-256이 변경되지 않는다.

이 조건이 완료되기 전에는 “OAuth 포팅 완료”, “현재 앱에서 Codex 동작 완료” 또는 “6시간 자동 요약 완료”라고 보고하지 않는다.
